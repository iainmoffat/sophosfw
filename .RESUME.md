# sophosfw Phase 6 — resume notes

**Branch:** `main` (Phase 5 merged + tagged `v0.4.0-phase5`, commit 09638d2)
**Plan:** `docs/superpowers/plans/2026-05-01-sophosfw-phase6.md` (12 tasks)
**Spec:** `docs/superpowers/specs/2026-05-01-sophosfw-phase6-design.md`
**Pacing rule:** subagent-driven-development. Implementer + spec review for every task. Quality review for tasks with real logic (T1, T2, T5, T6, T7, T8, T10). Skip quality review for trivial mechanical tasks (T3 envelope builders, T4 catalog field, T9 cli wiring) and for the final docs/acceptance task (T12). T11 is mostly skill-content edits + small skill-doctor extension; one quality review on the doctor change.

## Done so far (4/12)

| # | Task | Commit | Status |
|---|---|---|---|
| T1 | audit log + config knob | 87e98f4 | ✅ APPROVED |
| T2 | DiffHash (SHA-256 over canonical JSON) | 02fe6f8 | ✅ APPROVED |
| T3 | sophos BuildSetEnvelope + BuildRemoveEnvelope | 557ed72 | ✅ |
| T4 | catalog Mutable field + IPHost flagged mutable | 5170f19 | ✅ |

## Plan bugs encountered

(none yet)

## Next up (T5)

`HostIPSvc.Create` with validation, pre-flight read-only check, catalog mutable check, dry-run path, audit logging, refetch on apply. Plan section is Task 5. Full review.

## Remaining tasks roadmap (8 tasks)
- T2: Diff hash (full review)
- T3: Sophos Set/Remove envelope builders (trivial)
- T4: Catalog Mutable field (trivial)
- T5: HostIPSvc.Create + validation + audit (full review)
- T6: HostIPSvc.Update/Delete with diff hash (full review)
- T7: RawSvc.Apply real impl (full review)
- T8: cli host ip create/update/delete (full review)
- T9: cli raw request --yes --confirm-mutating (trivial)
- T10: MCP host_ip mutating tools (full review)
- T11: skill content + skill-doctor expansion (light review)
- T12: integration smoke + docs + acceptance + tag v0.5.0-phase6 (main session)

## Subagent dispatch pattern

Use `general-purpose` agents at `model: haiku` for implementer and spec review. Use `superpowers:code-reviewer` at `model: haiku` for quality review.

Phase 6 is the FIRST mutating phase. Pay extra attention to:
- pre-flight read-only-profile checks (every mutation method)
- audit log entries on success AND failure paths
- diff-hash mismatch handling
- catalog `Mutable` check (forward-compat for Phase 7+)

For each task:
1. Read plan section.
2. Dispatch implementer with full task text. Tell them: no Co-Authored-By trailer, single passing commit per task.
3. Wait for implementer report. If DONE, dispatch spec reviewer.
4. If spec ✅, dispatch quality reviewer (skip for trivial tasks).
5. If quality ✅ (or APPROVED WITH CONCERNS), mark complete here, advance.

## Conventions enforced

- No Co-Authored-By trailer on implementation commits.
- Single passing commit per task (tests pass at the moment of the commit).
- SDK alias: `sdkmcp "github.com/modelcontextprotocol/go-sdk/mcp"`.
- Module: `github.com/iainmoffat/sophosfw`. Working dir: `/Users/ipm/code/sophosfw`.
- Canonical skill files in `/Users/ipm/code/ai-tooling/skillshare/...` are NOT committed in sophosfw repo (T11 only commits skill-doctor source/tests).
- DO NOT commit `.RESUME.md` — keep it untracked.
- Push to origin after the phase tag is set (T12).

## Resume command

The user can prompt: "Continue executing the sophosfw Phase 6 plan from T<N>. Read /Users/ipm/code/sophosfw/.RESUME.md for context."
